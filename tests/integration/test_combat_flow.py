"""Integration tests for the combat flow. Phase-0 placeholder."""

from __future__ import annotations

import pytest


@pytest.mark.integration
def test_combat_flow_placeholder():
    """End-to-end combat flow lives here once Phase 1 is in.

    Skipped intentionally until the vertical slice exists. Marked `integration`
    so it's discoverable but doesn't fail CI.
    """
    pytest.skip("Combat flow lands in Phase 1")
